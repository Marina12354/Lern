if (условие 1) {
    вариант 1
} else if (условие 2) {
    вариант 2
} else if (условие 3) {
    вариант 3 
} else {
    вариант если вс условия НЕ выполнены
}    
for (let i = 0 /* создаем счетчик с начальным значением */; i < answer.length /* сравнени */; i++ /*изменениесчетчика */) {
    повторяемые действия
}
break

continue

while (условие) {
    повторяемые действия
}
do {
    повторяемые действия
} while (условие);

for ... of
for ... in


switch (проверяем значение) {
    case значение1:
        действия1
    case значение2:
    case значение3:
        действия2
        break;
    case значение4:
        действия3
    default:
        действия_по_умолчанию 
   
}